version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: dewit-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dewit
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
    container_name: dewit-backend
    environment:
      PORT: 3001
      DB_USER: postgres
      DB_HOST: postgres
      DB_NAME: dewit
      DB_PASSWORD: postgres
      DB_PORT: 5432
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
    command: npm start

  frontend:
    build:
      context: .
    container_name: dewit-frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data:
